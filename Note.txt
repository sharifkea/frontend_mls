python -m venv venv
source venv/Scripts/activate

pip install flask flask-cors requests python-dotenv cryptography
pip install -r requirements.txt

python app.py


#for electron
# Check if Node.js is installed
node --version
npm --version

# 1. Create package.json if not exists
npm init -y

# 2. Install Electron
npm install electron --save-dev

# 3. Run app
npm start

#When we're ready to distribute our app:
# Install electron-builder
npm install electron-builder --save-dev

# Build for Windows
npm run build -- --win

# Build for macOS
npm run build -- --mac

# Build for Linux
npm run build -- --linux

